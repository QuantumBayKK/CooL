/**
 * `cool-tee` — an alias for `cool-nwc`.
 *
 * The library lives under `cool-nwc`. This package exists so the adjacent name
 * resolves to the same code rather than to a 404 or, worse, to somebody else's
 * package. It carries no implementation of its own: one library, one place to
 * fix a bug.
 */
export * from "cool-nwc";
