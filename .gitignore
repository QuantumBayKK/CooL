# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
.vscode/*
!.vscode/extensions.json

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env*
!.env.example

# typescript
*.tsbuildinfo

# Large media. NOTE: clip ignores are intentionally disabled so the Vercel CLI
# uploads the footage with the deployment. For the long term, host video on a
# CDN / Vercel Blob and re-enable these (see PRD FR-15 / deploy notes).
# /public/clips/**/*.mp4
# /public/clips/**/*.webm
# /public/clips/**/*.mov
/public/audio/**/*.mp3
/public/audio/**/*.wav
/public/audio/**/*.ogg

.vercel

# Local snapshots taken before big rewrites — not part of the project.
/_backup_*/


# The CLI's per-project working directory: receipts, the transparency log
# and the update-check cache. Evidence belongs to the project it describes,
# not to this repository.
.cool/
